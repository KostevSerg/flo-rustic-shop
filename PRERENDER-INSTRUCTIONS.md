# Инструкция по генерации статичных HTML для SEO

## Проблема
Яндекс и другие поисковики видят только базовый HTML без динамических meta-тегов, которые генерируются React после загрузки.

## Решение
Создаём статичные HTML-файлы с правильными title и description для каждой страницы при билде.

## Как использовать

### 1. Запустите обычный билд проекта:
```bash
npm run build
# или
bun run build
```

### 2. Запустите скрипт генерации статичных страниц:
```bash
node scripts/generate-static-pages.js
```

Скрипт:
- Загрузит все города и товары из API
- Создаст HTML-файлы с правильными meta-тегами для:
  - **~250 страниц городов** (dist/city/[slug]/index.html)
  - **~40 страниц товаров** (dist/product/[id]/index.html)
  - **5 статичных страниц** (catalog, delivery, about, contacts, reviews)

### 3. Результат

После выполнения будет создана структура:
```
dist/
├── index.html (главная)
├── city/
│   ├── moskva/index.html
│   ├── biysk/index.html
│   └── ...
├── product/
│   ├── 7/index.html
│   ├── 8/index.html
│   └── ...
├── catalog/index.html
├── delivery/index.html
└── ...
```

Каждый файл будет иметь:
- ✅ Правильный title для конкретной страницы
- ✅ Правильный description
- ✅ Canonical URL
- ✅ Работающий React-код (для пользователей)

## Автоматизация

### Вариант 1: Добавить в package.json scripts (если возможно)
```json
{
  "scripts": {
    "build": "vite build",
    "postbuild": "node scripts/generate-static-pages.js"
  }
}
```

### Вариант 2: Создать единую команду
```bash
npm run build && node scripts/generate-static-pages.js
```

### Вариант 3: В CI/CD pipeline (GitHub Actions)
```yaml
- name: Build
  run: npm run build
  
- name: Generate static pages
  run: node scripts/generate-static-pages.js
  
- name: Deploy
  run: # ваша команда деплоя
```

## Проверка

После генерации проверьте файлы:

```bash
# Проверить страницу города
cat dist/city/biysk/index.html | grep "<title>"
cat dist/city/biysk/index.html | grep "description"

# Проверить страницу товара
cat dist/product/35/index.html | grep "<title>"
```

Должны увидеть правильные title и description!

## Важно!

- Скрипт использует данные из API, поэтому требуется интернет
- Генерация занимает ~10-30 секунд в зависимости от количества страниц
- Файлы создаются автоматически, не нужно их коммитить в Git (они будут в dist/)
- После деплоя поисковики увидят правильные meta-теги!

## Как это работает?

1. **Билд создаёт** базовый dist/index.html с общими meta-тегами
2. **Скрипт читает** этот файл
3. **Для каждой страницы** скрипт:
   - Заменяет title
   - Заменяет description  
   - Добавляет canonical URL
   - Сохраняет как отдельный HTML-файл
4. **Веб-сервер отдаёт** правильный HTML ботам
5. **React берёт управление** после загрузки для пользователей

Поисковики счастливы ✅  
Пользователи счастливы ✅  
SEO работает! 🚀
